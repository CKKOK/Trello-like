describe("Main script", function() {
  
  it("should fetch card and column data", function() {
  });

  it("should map the card and column data into their respective class instances", function() {
  });

  it("should filter cards according to whether a given search string is present in their titles or descriptions", function() {
  });

  it("should be able to reset all cards' visibility to being shown if the search string is empty", function() {
  });

  it("should be able to save the current view as a json file to be downloaded", function() {
  });

  it("should be able to create card data in the data store", function() {
  });

  it("should be able to update card data in the data store", function() {
  });
  
  it("should be able to delete card data in the data store", function() {
  });

  it("should be able to create column data in the data store", function() {
  });

  it("should be able to update column data in the data store", function() {
  });
  
  it("should be able to delete column data in the data store", function() {
  });
});

describe("Columns", function() {
  
  it("should be able to create a column", function() {
  });

  it("should be able to update a column's title", function() {
  });

  it("should be able to add a card to a column", function() {
  });

  it("should be able to drag and drop a card from one column to a different column", function() {
  });

  it("should be able to delete a column", function() {
  });

  
});

describe("Cards", function() {
  it("should be able to create a card", function() {
  });

  it("should be able to update a card's title", function() {
  });

  it("should be able to update a card's description", function() {
  });

  it("should be able to delete a card", function() {
  });

  it("should be able to search for a card containing a given string", function() {
  });

  it("should be able to expand a card's content", function() {
  });
})